# FILE LOCALE SENZA CREDENZIALI.
# Il Gestionale Ordini (Database) funziona senza compilare nulla qui.
#
# GOOGLE CALENDAR (facoltativo)
# La scheda Monitoraggio puo' usare Google Calendar solo se configuri
# manualmente un service account valido con la sezione [gcp_service_account].
#
# AUTOMAZIONE ARCHIVIO INNOVA (facoltativa, richiede Internet)
# Non distribuire mai refresh_token, client_secret o altri segreti.
# Se vuoi usare Automazione su QUESTO PC, compila localmente:
#
# [innova_drive_oauth]
# client_id = "..."
# client_secret = "..."
# refresh_token = "..."
#
# [innova_drive_real]
# root_id = "..."
# inbox_id = "..."
# root_name = "01 CLIENTI"
# writes_enabled = false
#
# Dopo una verifica in sola lettura puoi impostare writes_enabled = true.
